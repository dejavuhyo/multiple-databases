<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.multiple.mapper.primary.PrimaryMapper">

    <select id="getOracleList" resultType="com.example.multiple.dto.PrimaryDTO">
        SELECT GOODS_CD
             , GOODS_NM
        FROM GOODS_TB
    </select>

</mapper>
